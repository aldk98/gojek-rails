<%= form_with(model: minuman, local: true) do |form| %>
  <% if minuman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(minuman.errors.count, "error") %> prohibited this minuman from being saved:</h2>

      <ul>
      <% minuman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
